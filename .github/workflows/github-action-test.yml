# ワークフローの名前
name: github Action test

# developにマージされたときにjobsの内容が動作する
on:
  pull_request:
    branches:
      - develop
    types: [closed]

# 動かす内容
jobs:
  github-action-test:
    runs-on: ubuntsu-latest
    steps:
      - run: echo "Hello World!"
